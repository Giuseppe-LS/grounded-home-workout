const workouts=[{id:'a',name:'Back & Glutes',focus:'Posterior chain • posture',ex:[['Glute Bridge','2–3 × 12–15','Squeeze at the top for 2 seconds.','◡'],['Band Romanian Deadlift','2–3 × 10–12','Push hips back; keep spine neutral.','↙'],['Bird Dog','2–3 × 8/side','Slow movement; keep hips level.','✣'],['Resistance Band Row','2–3 × 12–15','Pull elbows back; shoulders relaxed.','⇐'],['Reverse Lunge','2–3 × 8/leg','Step back and stay controlled.','↘'],['Dead Bug','2 × 8–10','Keep lower back gently grounded.','✦'],['Front Plank','2 × 20–30 sec','Long straight line; breathe.','━']]},{id:'b',name:'Legs & Balance',focus:'Lower body • stability',ex:[['Bodyweight Squat','2–3 × 12','Sit back; chest comfortably tall.','⌄'],['Supported Split Squat','2–3 × 8/leg','Hold a chair if needed; small range is fine.','⇵'],['Lateral Band Walk','2–3 × 10/side','Keep gentle band tension throughout.','↔'],['Single-leg Romanian Deadlift','2–3 × 8/leg','Use a wall for balance if needed.','⚖'],['Standing Calf Raise','2 × 15–20','Pause briefly at the top.','↑'],['Side Plank','2 × 15–20 sec/side','Begin from knees if needed.','◢'],['Single-leg Balance','2 × 30 sec/leg','Soft knee; focus on one point.','●']]},{id:'c',name:'Full Body Flow',focus:'Whole body • coordination',ex:[['Step-ups','2–3 × 10/leg','Use a stable low step; drive through whole foot.','↗'],['Incline Push-up','2–3 × 8–12','Hands on stable counter/sofa; body long.','➚'],['Resistance Band Pull Apart','2–3 × 12–15','Keep shoulders away from ears.','↔'],['Hip Thrust','2–3 × 12–15','Squeeze glutes; avoid arching lower back.','⌒'],['Bear Hover Hold','2 × 15–20 sec','Knees just off floor; breathe steadily.','▰'],["Child's Pose",'1–2 min','Relax and breathe slowly.','⌣']]}];
const KEY='grounded-v2';let state=load();let current=null;function load(){try{return JSON.parse(localStorage.getItem(KEY))||{sessions:[],weights:[]}}catch(e){return{sessions:[],weights:[]}}}function save(){localStorage.setItem(KEY,JSON.stringify(state))}function show(id){document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));document.getElementById(id).classList.add('active');document.querySelectorAll('nav button').forEach(b=>b.classList.toggle('nav-active',b.dataset.go===id));if(id==='home')renderHome();if(id==='progress')renderProgress();scrollTo(0,0)}document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>show(b.dataset.go));
function renderHome(){const c=document.getElementById('workoutCards');c.innerHTML='';workouts.forEach(w=>{let d=document.createElement('div');d.className='card';d.innerHTML=`<span class="tag">~30 min</span><h3>${w.name}</h3><p>${w.focus}</p>`;d.onclick=()=>openWorkout(w.id);c.appendChild(d)});let now=new Date(),start=new Date(now);start.setDate(now.getDate()-((now.getDay()+6)%7));start.setHours(0,0,0,0);weekCount.textContent=state.sessions.filter(s=>new Date(s.date)>=start).length;latestWeight.textContent=state.weights.length?state.weights[state.weights.length-1].value+' kg':'—'}
function openWorkout(id){current=workouts.find(w=>w.id===id);workoutTitle.textContent=current.name;workoutMeta.textContent='About 30 minutes • Beginner friendly • 2 rounds to start';exerciseList.innerHTML=current.ex.map(e=>`<article class="exercise"><div class="visual">${e[3]}</div><div><h4>${e[0]}</h4><p><b>${e[1]}</b></p><p>${e[2]}</p></div></article>`).join('');sessionNotes.value='';difficulty.value='Good';show('workout')}
completeWorkout.onclick=()=>{if(!current)return;state.sessions.push({id:Date.now(),workout:current.name,date:new Date().toISOString(),difficulty:difficulty.value,notes:sessionNotes.value.trim()});save();alert('Workout saved ✓');show('home')};
saveWeight.onclick=()=>{let v=parseFloat(weightInput.value);if(!v||v<30||v>250){alert('Please enter a valid weight.');return}state.weights.push({id:Date.now(),date:new Date().toISOString(),value:v});state.weights.sort((a,b)=>new Date(a.date)-new Date(b.date));save();weightInput.value='';renderProgress()};
function renderProgress(){weightHistory.innerHTML=state.weights.length?[...state.weights].reverse().map(w=>`<div class="history-item"><b>${w.value} kg</b> · ${fmt(w.date)} <button onclick="delWeight(${w.id})">delete</button></div>`).join(''):'<p class="muted">No weight entries yet.</p>';sessionHistory.innerHTML=state.sessions.length?[...state.sessions].reverse().map(s=>`<div class="history-item"><b>${s.workout}</b><br><span class="muted">${fmt(s.date)} · ${s.difficulty}${s.notes?' · '+esc(s.notes):''}</span></div>`).join(''):'<p class="muted">Complete a workout and it will appear here.</p>';drawChart()};function delWeight(id){state.weights=state.weights.filter(x=>x.id!==id);save();renderProgress()}window.delWeight=delWeight;function fmt(d){return new Date(d).toLocaleDateString(undefined,{day:'numeric',month:'short',year:'numeric'})}function esc(s){let d=document.createElement('div');d.textContent=s;return d.innerHTML}
function drawChart(){let cv=weightChart,x=cv.getContext('2d'),W=cv.width,H=cv.height;x.clearRect(0,0,W,H);if(state.weights.length<2){x.fillStyle='#737b75';x.font='20px system-ui';x.fillText('Add 2+ entries to see your trend',30,H/2);return}let vals=state.weights.map(w=>w.value),min=Math.min(...vals)-.5,max=Math.max(...vals)+.5,p=35;x.strokeStyle='#b8c5ba';x.lineWidth=2;x.beginPath();vals.forEach((v,i)=>{let px=p+i*(W-2*p)/(vals.length-1),py=H-p-(v-min)/(max-min)*(H-2*p);i?x.lineTo(px,py):x.moveTo(px,py)});x.stroke();x.fillStyle='#2f463a';vals.forEach((v,i)=>{let px=p+i*(W-2*p)/(vals.length-1),py=H-p-(v-min)/(max-min)*(H-2*p);x.beginPath();x.arc(px,py,5,0,Math.PI*2);x.fill()})}
let deferredPrompt;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;installBtn.hidden=false});installBtn.onclick=async()=>{if(deferredPrompt){deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;installBtn.hidden=true}};if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js');renderHome();